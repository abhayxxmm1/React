# Notes for the react 

```javascript
// src is the main folder where all the react code will be located
// component's are the fucntion's with additonal power's
/* 
function
    call a function
    pass parameters
    arguments
    resues
    return value
    set of instructions
    block of code
    define
*/
// component is a funciont that returns u a HTML like syntax called JSX ( javascript xml language ) ( HTML like syntax ) 
// Import statementnpx 
// src is the main folder where all the react code will be located
// component's are the fucntion's with additonal power's
/* 
function
    call a function
    pass parameters
    arguments
    resues
    return value
    set of instructions
    block of code
    define
*/
// component is a funciont that returns u a HTML like syntax called JSX ( javascript xml language ) ( HTML like syntax ) 

// npx & npm dont have official naming but called as node package manager

// Their are two types of export
// export default (max 1) & another is normal export 

//  React is syncrhonous

// console.log are the best debuggers

```