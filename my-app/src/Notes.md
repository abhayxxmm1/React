# Notes for the react 

```javascript
// src is the main folder where all the react code will be located
// component's are the fucntion's with additonal power's
/* 
function
    call a function
    pass parameters
    arguments
    resues
    return value
    set of instructions
    block of code
    define
*/
// component is a funciont that returns u a HTML like syntax called JSX ( javascript xml language ) ( HTML like syntax ) 
// Import statement

```